# Исторический файл: активная конфигурация nginx для прод-сайта — `docker/nginx-default.conf`
# внутри корневого `Dockerfile` (образ PHP-FPM + nginx + supervisord).
# Не подключайте этот файл в рабочий nginx: здесь намеренно нет директив `server {}`.
