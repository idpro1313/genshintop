# Каталог `content/characters/` (пустой намеренно)

Карточки персонажей на сайте читаются из **`info/characters/*.md`** (`ContentRepository`, Docker копирует `info/` в образ).

Резерв длинного текста до пересборки — **`content/characters-archive/`**. Дублировать Markdown сюда не нужно.
