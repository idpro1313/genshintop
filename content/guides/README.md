# Каталог `content/guides/` (пустой намеренно)

Опорные гайды сайта читаются из **`info/guides/*.md`** (`ContentRepository`, Docker копирует `info/` в образ).

Прежний массовый корпус после рефактора лежит в **`content/guides-archive/`**. Дублировать Markdown сюда не нужно.
